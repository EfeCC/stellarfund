# Normalise to LF in the repository. The Makefile and the deploy scripts run on
# the Linux CI runners, where CRLF would break them.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.wasm binary
