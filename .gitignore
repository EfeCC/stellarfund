# Rust's output directory
target

# soroban-sdk rewrites these on every `cargo test`. Committing them would put a
# few dozen churning JSON files in the diff of any change that touches a contract.
test_snapshots/

# Local settings
.soroban
.stellar

# Node
node_modules
dist
.vite
coverage

# Environment / secrets
.env
.env.local
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Vercel
.vercel
